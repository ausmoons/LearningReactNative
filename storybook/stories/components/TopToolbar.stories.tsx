import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { TopToolbar } from '../../../src/components';

storiesOf('TopToolbar', module).add('default', () => <TopToolbar />);
