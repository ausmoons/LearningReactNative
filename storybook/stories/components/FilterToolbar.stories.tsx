import React from 'react';
import { storiesOf } from '@storybook/react-native';
import { FilterToolbar } from '../../../src/components';

storiesOf('FilterToolbar', module).add('default', () => <FilterToolbar />);
